# jev-poc（Jev 自動早押し）

読み上げ中の問題文を [Jev](https://docs.typesafe.ai/) へ随時投げ、「今が確定ポイントか」を判定して、**人間の操作なしに早押しボタンを押す** PoC。

押した側が答える。**Jev が押せば LLM が回答し、人間が押せば人間が回答する**ため、人間 vs AI の早押し対決になる。

凍結 [`poc/`](poc.md) が「早押しボタンを押した後」を自動化していたのに対し、こちらは**ボタンを押すこと自体**を自動化する。

出題データは [quiz-data.md](quiz-data.md)。

## 起動

`server` が起動している必要がある（未起動でも手動早押しは動く）。

```bash
cp server/.env.example server/.env
```

`server/.env` に `TYPESAFE_API_KEY` を設定する。
AI に回答させる場合は、あわせて `ANTHROPIC_API_KEY` / `GEMINI_API_KEY` / `XAI_API_KEY` も設定する（[LLM 予測](llm.md)と同じキー）。

```bash
cd server && uv run uvicorn app.main:app --port 8000
```

別のターミナルで:

```bash
npm run dev -w jev-poc
```

`dev` / `build` は、実行前に `questions.csv` を読んで `manifest.json` を自動生成する。
データを追加・削除した際は再実行する。

## 操作

1. 「開始」を押すとジングルが鳴り、続いて問題音声が再生される
2. 再生に合わせて問題文が表示されていく
3. 先に押した側が答える
   - **Jev が押した場合** — 3 モデルが並列に回答し、合議で 1 つの答えを決める
   - **人間が押した場合** — 答えを入力して「回答」を押す
4. 「次へ」で次の問題へ

**AI が外したら、読み上げが再開されて人間へ解答権が移る。**
実際の早押しクイズと同じく、誤答した側から他者へ権利が移る形にしている。
読み上げは止めた位置から続く（最初から読み直すと AI が押した意味が無くなるため）。

人間が外した場合は、そのまま結果表示へ進む。AI は押す判断を自分で行うため、
権利を渡す先として扱えない。

「自動で押す」のチェックを外すと、従来どおり**スペースキー**または画面中央のボタンで手動早押しできる。
`server` 未起動や `TYPESAFE_API_KEY` 未設定の場合は自動早押しが無効になり、手動のみで動く。

### 画面の見かた

自動で押されると、押した理由と判定位置が表示される。

```
Jev が押しました（31 字時点の判定）: buzz 0.87 / 31 字で危険区間を抜けている
```

問題文の色は 2 段階になる。

| 色 | 意味 |
| --- | --- |
| 通常色 | Jev が判定に使った範囲 |
| 灰色 | 判定から押下までの間に読み進んだぶん。**Jev は見ていない** |

灰色の長さがそのまま**判定の遅れ**である。押すのが遅い原因が、判定の遅さなのか閾値の高さなのかを切り分ける手がかりになる。

判定の時系列も表示され、**押さなかった場合の理由も残る**。閾値を調整する際の材料になる。

## AI の回答

Jev が押すと、その時点で**実際に読み上げられていた**問題文を 3 モデルへ並列に投げる。

| vendor | model | 表示 |
| --- | --- | --- |
| anthropic | `claude-sonnet-5` | Claude |
| google | `gemini-3.7-flash` | Gemini |
| xai | `grok-4.3` | Grok |

モデルは固定で、画面からは選べない。対決の相手を毎回同じにすることで勝敗の比較が成立する
（`llm-poc` の枠選択はモデル間の比較実験のための機能であり、目的が違う）。

### 渡すのは「読み上げられた部分まで」

Jev が判断した位置（灰色表示の手前）ではなく、**画面に出ている範囲すべて**を渡す。

判定から押下までには数百ミリ秒あり、その間も読み上げは進む。人間が同じ位置で押した場合にも
同じ範囲が見えているため、AI にだけ狭い範囲を渡すと対決の条件が不利になる。

### 合議

3 モデルの応答から 1 つの答えを決める。

1. **2 モデル以上が同じ回答** → それを採用
2. **3 モデルがそれぞれ異なる** → **最初に回答を出したモデル**を採用
3. 一部が失敗（エラー・応答フォーマット違反・タイムアウト）→ 成功したモデルだけで 1〜2 を適用
4. **全モデルが失敗** → 回答なし。AI の誤答として扱い、人間へ権利が移る

「最初に回答を出した」は `/predict` が返す**サーバ実測の `elapsed_ms`** で測る。
フロントへの到着順はネットワークのゆらぎで変わり、モデルの速さを表さないため採らない。

### 多数決が決まった時点で打ち切る

**2 モデルが一致したら、3 モデル目を待たずに確定する。** 残る 1 モデルが何を答えても
一致している 2 票は超えられないため、待っても結果は変わらない。
早押しでは回答までの速さも勝敗に関わるので、決まっている応答を待たない。

実測では 2299ms で確定し、3 モデル目は 6399ms に届いた（**4.1 秒の短縮**）。

遅れて届いた応答も「AI の回答」へ反映する。勝敗には影響しないが、
どのモデルが何を答えたかは記録として残す価値があるため。

打ち切るのは**結果が覆らないと分かる場合だけ**である（`canSettleEarly`）。
2 モデルが異なる回答をした場合は、3 モデル目がどちらかに付けば多数決、
違う答えなら最速採用となり、どちらになるか分からないため待つ。

同じ回答かどうかは、正誤判定と同じ正規化で比べる。括弧類も落とす
（実測で Claude と Grok が「檸檬」、Gemini が「『檸檬』」を返した。括弧を残すと
実質同じ答えなのに多数決が割れる）。

### 正誤判定は人間・AI とも同じ

`lib/answer.ts` の `isCorrect`（正規化した上での部分一致）を両者へ適用する。
対決では両者が同じ条件で判定される必要があり、片方だけ緩い／厳密では公平でない。

`llm-poc` は完全一致＋手動正解ボタンだが、こちらは採らない。
対決の進行中に手動正解ボタンを押す操作を挟みたくないため。

## 押下判定

### 規則

```
buzz ≧ 0.90                          → 押す（asking を見ない）
buzz ≧ 0.70
  ├ 30 字超 または「ずばり」を含む     → 押す
  └ 30 字以下                         → asking ≧ 0.60 を要求
```

閾値は [`server/app/jev/decision.py`](../server/app/jev/decision.py) と [`jev-poc/src/lib/decision.ts`](../jev-poc/src/lib/decision.ts) の**両方**に定義がある。
計測スクリプトはサーバ側を、画面は TypeScript 側を使うため、**片方だけ変えると計測結果と実機の挙動がズレる**。必ず両方を直すこと。

### 質問

1 リクエストに 4 問を混ぜ、並列に評価させる（Jev は質問を増やしても応答時間がほぼ変わらない）。

| id | 型 | 用途 |
| --- | --- | --- |
| `buzz` | noul | **押下判定。** 残りを聞かずに答えられるか |
| `asking` | noul | 既に問いの本体に入っているか。パラレル問題を転換前に弾く |
| `parallel` | noul | パラレル問題か。**押下には使わず**、記録として残す |
| `narrowed` | score | 候補の絞られ具合。**押下には使わず**、閾値調整に使う |

`narrowed` の score は **0〜1 ではなく 0〜3**（段階数 - 1）。`/api/jev/health` が `narrowed_levels` で段階数を返す。

### なぜこの形なのか

**特徴を 10 個ほど並列に測って閾値を超えたら押す、という形は採らない。**
Jev の公式が分解を勧めているのは「1 つの質問が複数の独立要素を含む場合」であり、「今押せるか」は単一の判断である。特徴を分解しても最後は「押すか」へ畳まれるうえ、閾値の調整対象が増える。

並列の枠は、押下判定とは**独立した情報を持つ質問**に使っている。

**パラレル問題（「A は X ですが、B は Y でしょう？」）が最大の事故要因である。**
前半で `buzz` が高く出るが、そこで押すと誤答になる。

当初は `parallel` で弾く設計だったが、計測すると **`parallel` は「ですが」が読まれた後に跳ね上がる事後検出器**だと分かった（前振り部 0.33 → 転換後 0.79）。転換語が出た時点では既に押すかどうかを決めた後であり、早押しには使えない。実測でも転換前の危険点を 0/8 件しか阻止できなかった。

そこで 2 つを組み合わせている。

1. **`asking`** — パラレル問題の前半は必ず平叙文である。「既に問いの本体に入っているか」を問えば、転換語を待たずに判定できる
2. **文字数** — `questions.csv` の全パラレル問題で「ですが」は 12〜26 字に現れる。長い前置きの後に転換を置くと問題文全体が長くなりすぎるためで、出題形式に由来する構造的な性質である

文字数で危険区間を 30 字に限ることで、`asking` を課す範囲を絞れる。

`buzz >= 0.70` で比較した場合（`asking` の閾値は 0.50 での比較）:

| | 危険点の阻止 | 非パラレルの押下 | 平均押下位置 |
| --- | --- | --- | --- |
| ゲートなし | 0/8 | 5/5 問 | 49.3% |
| `asking` 常時 | 5/8 | 4/5 問 | 78.6% |
| **文字数で限定** | **5/8** | **5/5 問** | **65.2%** |

阻止性能を落とさずに、確定点が後半にある問題が本来の位置で押せるようになる。

なお `ASKING_MIN` を 0.60 とした最終形では、転換前の危険点 **8/8 件**を阻止する。
上の表の 5/8 は、`asking` の閾値が 0.50 だったときの数字である。

読点の数（目的語・補語の多さ）による判定は**採らない**。パラレル問題の「ですが」前の読点数は 0〜1、非パラレル問題の先頭 26 字も平均 0.8 で分離しない。前置きが短いという特徴が既に読点の少なさを含んでおり、文字数と独立した情報にならない。

### 現在の成績

11 問（うちパラレル 6 問）での計測結果。

```
押下 7/11 問  平均押下位置 68.7%  転換前の誤押し 0 件
```

押さなかった 4 問はすべてパラレル問題である。パラレル問題は転換後の後半に本当の確定点が来るため、そこまで `buzz` が上がらなければ押さないのが正しい。

> [!NOTE]
> **閾値の根拠は 11 問しかない。**
> 危険区間 30 字はパラレル 6 問（最大 26 字）から決めており、26 字を超えて転換する問題が出れば破れる。
> 「ずばり」の規則は現データに該当が 0 問で、**効果を確認できていない**。

## テスト

```bash
npm test -w jev-poc     # 押下規則・非同期制御・合議・状態遷移
cd server && uv run pytest
```

押下規則は Python と TypeScript の**両方**にテストがある
（`server/tests/test_jev_decision.py` と `jev-poc/src/lib/decision.test.ts`）。
同じケース・同じ期待値を並べてあり、片方だけ変えるとどちらかが落ちる。

境界値はテスト内に直接書いてある。定数を参照すると、閾値を変えたときに
テストも一緒に動いてしまい、変更に気づけないため。

`useAutoBuzz` のテストは、判定の応答が届いたときに状況が変わっていた場合
（次の問題へ進んだ、人間が先に押した、自動を切った、読み切った）の挙動を
固定している。いずれも型では落ちない経路である。

## 閾値の調整

問題文を 1 文字ずつ伸ばしながら判定を投げ、確率の推移（確定曲線）を記録する。

```bash
cd server
uv run python scripts/measure_buzz_curve.py --limit 5
```

| オプション | 内容 |
| --- | --- |
| `--limit` | 評価する問題数（既定: 全問） |
| `--step` | 何文字ごとに評価するか（既定: 1）。API 呼び出しを減らすなら増やす |
| `--variant` | 使う質問定義（既定: `v3`） |
| `--only-parallel` | 「ですが」を含む問題だけを測る |

**1 問あたり（文字数 ÷ step）回 API を叩く。** まず `--limit` を小さくして感触を見るとよい。

結果は `server/logs/buzz_curve.csv` へ**追記**される（`.gitignore` 対象）。
`variant` 列で区別するため、質問定義を変えた計測を 1 ファイルに貯められる。

集計する。

```bash
uv run python scripts/analyze_buzz_curve.py
```

**集計側は API を叩かない。** 閾値を変えて試すたびに課金される形にしないため。

出力には次が含まれる。

- `buzz` の上限（全文を読み切った時点の値）
- 閾値の組み合わせごとの押下率・押下位置
- **実装された押下規則での成績** — `decision.py` の `decide()` を直接呼ぶため、実機で起きることと一致する
- 転換前の誤押しと、各ゲートの阻止数

### 質問定義を変える場合

[`server/app/jev/questions.py`](../server/app/jev/questions.py) に variant として足し、`VARIANTS` へ登録する。
既存の定義を書き換えず variant を増やすのは、**同じ問題セットで前後を比較できるようにする**ため。

`/api/jev/judge` が実際に使うのは `ACTIVE_QUESTIONS`（現在は `v3`）である。

## 仕組み

### 送信のタイミング

**表示文字数が変化したときだけ**送る。時間で回すと、無音区間や長音で同じ文字列を繰り返し投げることになる。

応答待ちの間は送信しない（in-flight は常に 1 本）。
待っている間に読み上げが進むことは**許容する**。人間の早押しでも判断から押下までに時間差は生じる。

### 音声と問題文の同期

凍結 `poc/` と同じ方式（`.lab` の無音区間で分割し、読点で対応付ける）。
詳細は [poc.md](poc.md#仕組み--音声と問題文の同期) を参照。

### `poc/` からのコピーについて

`align.ts` と `lab.ts` は `poc/` からのコピーで、`web/` に続く**三重化**になっている。

共通化していないのは、`jev-poc` が実験場であり、自動早押しの精度を見るなかで同期ロジックに手を入れる可能性があるため。
凍結 `poc/` と、実際の開催で使う `web/` を巻き込むリスクの方が大きい。

## API

`server/` が TypeSafe API を中継する。API キーをブラウザのバンドルへ埋め込めないため。

### `GET /api/jev/health`

```json
{ "available": true, "model": "jev-latest", "narrowed_levels": 4 }
```

`TYPESAFE_API_KEY` が未設定なら `available: false` と `reason` を返す。

### `POST /api/jev/judge`

```json
{ "partial_text": "「芸術は爆発だ」の名言で知られる" }
```

**問題文の全文と正解は送らない。** `jev-poc` が保持したままにする（`/api/llm/predict` と同じ原則）。

```json
{
  "ok": true,
  "buzz": 0.66,
  "parallel": 0.38,
  "asking": 0.15,
  "narrowed": 2.5,
  "narrowed_confidence": 0.5,
  "elapsed_ms": 559
}
```

**失敗しても HTTP 200 を返す。** 読み上げ中は次の文字で再送されるため、1 回の失敗はリクエストの失敗ではなく、その時点の観測が得られなかったという情報にすぎない。区別は `ok` と `error_kind` で行う。

質問が応答に含まれなかった場合、その値は `null` になる。既定値で埋めると「押すな（0）」と「値が無い」を取り違えるため。

応答時間の実測は **438〜830ms**。

## 環境変数

| 環境変数 | 用途 |
| --- | --- |
| `TYPESAFE_API_KEY` | Jev の API キー |
| `JEV_MODEL` | 使うモデル（既定 `jev-latest`） |

## ファイル構成

```
server/app/jev/
├── config.py       API キーとモデル名
├── client.py       TypeSafe API の呼び出しとエラー正規化
├── questions.py    質問定義（v1 / v2 / v3 と ACTIVE_QUESTIONS）
├── decision.py     押下判定（閾値の定義）
└── router.py       /api/jev/health, /api/jev/judge

server/scripts/
├── measure_buzz_curve.py    確定曲線の計測（API を叩く）
└── analyze_buzz_curve.py    集計と閾値の比較（API を叩かない）
```

```
jev-poc/
├── scripts/build-manifest.ts   questions.csv の検証と manifest.json の生成
└── src/
    ├── App.tsx                 画面全体と音声再生の制御
    ├── state/quizMachine.ts    出題サイクルの状態遷移
    ├── lib/
    │   ├── lab.ts              .lab パーサ（poc/ からのコピー）
    │   ├── align.ts            音声と問題文の対応付け（poc/ からのコピー）
    │   ├── answer.ts           正誤判定（poc/ からのコピー）
    │   ├── manifest.ts         問題の読み込みと抽選（poc/ からのコピー）
    │   ├── sound.ts            ジングル SE（poc/ からのコピー）
    │   ├── jev.ts              判定 API の呼び出し
    │   ├── decision.ts         押下判定（decision.py と同じ規則）
    │   ├── useAutoBuzz.ts      送信の間引きと押下
    │   ├── llm.ts              予測 API の呼び出し（llm-poc からの移植）
    │   ├── consensus.ts        3 モデルの応答から答えを 1 つ決める
    │   └── useAiAnswer.ts      3 モデルへの並列送信と合議
    └── components/
        ├── JevPanel.tsx        判定の時系列と自動 / 手動の切り替え
        └── AiAnswerView.tsx    AI の回答と合議の経緯
```
